<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta charset="utf-8">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>DaOAuth</title>
    @await Html.PartialAsync("_css")
    @RenderSection("Css", required: false)
</head>
<body>

    @*<nav class="navbar fixed-top navbar-dark bg-dark">
            <a class="navbar-brand" href="#">DaOAuth</a>
        </nav>*@

    <nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
        <a class="navbar-brand" href="#">DaOAuth</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarsExampleDefault">
            <ul class="navbar-nav mr-auto">
              
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="http://example.com" id="dropdown01" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Langage</a>
                    <div class="dropdown-menu" aria-labelledby="dropdown01">
                        <a class="dropdown-item" onclick="changeCulture('en')">English</a>
                        <a class="dropdown-item" onclick="changeCulture('fr')">French</a>
                    </div>
                </li>
            </ul>
        </div>
    </nav>

    <main role="main" class="container" style="margin-top:80px">
        @RenderBody()
    </main>

    @await Html.PartialAsync("_scripts")
</body>
</html>
