@model DaOAuthV2.Gui.Front.Models.CreateClientModel

@{
    Layout = "_fr.layout";
}

@await Html.PartialAsync("_fr.modelErrors")

<h1>Création d'un nouveau client</h1>
<hr />
<div class="row">
    <div class="col-md-8">
        <form asp-action="Create" method="post">
            <div class="form-group">
                <label asp-for="ClientType" class="control-label">Type de client</label>
                <br />
                @Html.RadioButtonFor(m => m.ClientType, "confidential") Confidential
                <br />
                @Html.RadioButtonFor(m => m.ClientType, "public") Public
            </div>
            <div class="form-group">
                <label asp-for="DefaultReturnUrl" class="control-label">Url de retour</label>
                @Html.TextBoxFor(model => model.DefaultReturnUrl, new { @class = "form-control" })
            </div>
            <div class="form-group">
                <label asp-for="Name" class="control-label">Nom</label>
                @Html.TextBoxFor(model => model.Name, new { @class = "form-control" })
            </div>
            <div class="form-group">
                <label asp-for="Description" class="control-label">Description</label>
                @Html.TextAreaFor(model => model.Description, 10, 80, new { @class = "form-control" })
            </div>
            <div class="form-group">
                <input type="submit" value="Submit" class="btn btn-lg btn-primary btn-block" />
            </div>
        </form>
    </div>
</div>

